import {
  Images,
  Heading,
  Ul,
  List,
  PlayAgainButton,
} from '../../StyledComponents'

const ResultView = props => {
  const {myChoice, opponentChoice, gameStatus, playAgain} = props
  const onPlayAgain = () => {
    playAgain()
  }
  return (
    
  )
}

export default ResultView
